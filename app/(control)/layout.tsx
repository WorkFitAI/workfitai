import { ControlLayoutWrapper } from "@/components/layout/control/control-layout-wrapper"

export default function ControlLayout({ children }: { children: React.ReactNode }) {
  return <ControlLayoutWrapper>{children}</ControlLayoutWrapper>
}
